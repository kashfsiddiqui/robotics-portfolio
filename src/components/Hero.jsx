import { motion } from 'framer-motion';

const Hero = () => {
  return (
    <section className="bg-white pt-20">
      {/* Empty hero section - intro text is now in the Projects grid */}
    </section>
  );
};

export default Hero;
